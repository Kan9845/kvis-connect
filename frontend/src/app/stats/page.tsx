import StatsClient from "./StatsClient";

export const metadata = {
  title: "By the Numbers · KVIS Connect",
  description:
    "An almanac of where KVIS alumni went to study after graduation — faculties, universities, and countries, by cohort.",
};

export default function StatsPage() {
  return <StatsClient />;
}
