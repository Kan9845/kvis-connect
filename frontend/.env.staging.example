# Use exactly one mode.

# Mode A: real staging API (recommended for authentication/admin verification)
BACKEND_URL=https://kvis-connect-staging-backend.onrender.com
NEXT_PUBLIC_USE_MOCK=false
NEXT_PUBLIC_DISABLE_AUTH=false

# Mode B: UI-only browser mock (no backend or database access)
# Remove BACKEND_URL and set NEXT_PUBLIC_USE_MOCK=true in your shell.
